user_phase = str(input("Enter a phrase: "))

user_phase_split = user_phase.split(" ")

phrase = ""
for word in user_phase_split:
    word[0] in user_phase_split
    phrase = phrase + word[0]

phrase_alpha = phrase.upper()

print(phrase_alpha)